<div class="d-flex flex-column">
  <div class="d-flex justify-content-center pb-2">
    <img src="<%= service.iconSrc %>" width="50" height="50" alt="<%= service.name %>" />
  </div>
  <div class="fs-4 fw-semibold text-center pb-2">
    <%= service.name %>
  </div>
  <div class="text-center">
    <%= service.description %>
  </div>
</div>